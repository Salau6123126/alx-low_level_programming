#include "main.h"
#include <stdio.h>

/**
 * print_to_98 - Prints all natural numbers from n to 98
 * @n - the first term
 * Return: Always (0)
 */
void print_to_98(int n)
{
	int i;

	for (i = n; i < 99; i++)
	{
		printf("%d", n);
		putchar(',');
	}
	purchar('\n');

	return (98);
}
